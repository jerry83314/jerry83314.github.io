<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Promise 搭配 $nextTick</title>
    <url>/2021/02/16/%E6%90%AD%E9%85%8D/</url>
    <content><![CDATA[<p>最近的一個專案中，切版的部分是外包，我要引入對方的 jQuery 套件和一些第三方 JS<br>正常會有幾個做法，最簡單的就是在 Vue Cli 的 public/index.html 直接貼上 script<br>另外也可以在 mounted hook 的時候引入這些東西<br>但是當東西太多，上面兩個方法都行不通的時候，我當下呈現這個表情：<br><img src="https://i.imgur.com/HHQ18B6.png" alt="Image"></p>
<hr>
<p>幾經波折跟同事研究之後，決定用 <code>$nextTick</code> 來處理，但還有牽扯到打 API 拿資料，這時候情況就會變得比較複雜了<br><img src="https://i.imgur.com/zYRAcqw.png" alt="Image"></p>
<hr>
<p>我們分了幾個步驟：</p>
<h5 id="把打-API-的-function-改成-return-promise"><a href="#把打-API-的-function-改成-return-promise" class="headerlink" title="把打 API 的 function 改成 return promise"></a>把打 API 的 function 改成 return promise</h5><h5 id="在成功打到-API-之後採用-nextTick"><a href="#在成功打到-API-之後採用-nextTick" class="headerlink" title="在成功打到 API 之後採用 $nextTick"></a>在成功打到 API 之後採用 $nextTick</h5><h5 id="挖出要執行的-jQuery-code-放在-nextTick-裡面"><a href="#挖出要執行的-jQuery-code-放在-nextTick-裡面" class="headerlink" title="挖出要執行的 jQuery code 放在 $nextTick 裡面"></a>挖出要執行的 jQuery code 放在 $nextTick 裡面</h5><h5 id="在-mouted-hook-引用-promise"><a href="#在-mouted-hook-引用-promise" class="headerlink" title="在 mouted hook 引用 promise"></a>在 mouted hook 引用 promise</h5><p>PS. 這裡會用 Promise.all 是因為不只打了一隻 API</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">mounted</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">  <span class="built_in">Promise</span>.all([<span class="built_in">this</span>.getData()])</span><br><span class="line">    .then(<span class="function">() =&gt;</span> &#123;&#125;)</span><br><span class="line">&#125;,</span><br><span class="line">methods: &#123;</span><br><span class="line">  <span class="function"><span class="title">getData</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">this</span>.axios.post(url, &#123;&#125;)</span><br><span class="line">        .then(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">          <span class="built_in">this</span>.$nextTick(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">            <span class="comment">// 要放的 jQuery code</span></span><br><span class="line">            resolve()</span><br><span class="line">          &#125;)</span><br><span class="line">        &#125;)</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure>
<hr>
<p>這樣就成功的解決了呢，我們研究了很久才終於搞定，真的是平安喜樂呢！<br><img src="https://i.imgur.com/fHL3aX7.png" alt="Image"></p>
<hr>
<p>今年因為疫情的關係，沒有辦法出國玩，卻不知不覺的學習了很多東西，也是得償所望啊<br>願我們能在技術這條路上，繼續走下去，共勉之。<br>最後祝大家：<br><img src="https://i.imgur.com/EplMtIc.png" alt="Image"></p>
]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Promise</tag>
        <tag>$nextTick</tag>
        <tag>Vue Cli</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 陣列隨機取得一筆資料</title>
    <url>/2021/03/28/%E9%99%A3%E5%88%97%E9%9A%A8%E6%A9%9F%E5%8F%96%E5%BE%97%E4%B8%80%E7%AD%86%E8%B3%87%E6%96%99/</url>
    <content><![CDATA[<p>不知道大家有沒有遇過一種情況，打 API 完之後取得的是一大包資料，卻只要其中的一筆，而這一筆在每一次進入頁面的時候都要隨機 Random，這時候可以使用 JavaScript 內的 Math.floor 搭配 Math.random 來處理</p>
<hr>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> ary = [<span class="string">&#x27;a&#x27;</span>, <span class="string">&#x27;b&#x27;</span>, <span class="string">&#x27;c&#x27;</span>];</span><br><span class="line"><span class="keyword">const</span> key = <span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.radom() * ary.length);</span><br><span class="line">ary[key]</span><br></pre></td></tr></table></figure>
<hr>
<p>這樣的一個小技巧，就可以達到我們的需求了，分享給大家！</p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Node 初體驗</title>
    <url>/2021/04/20/Node%20%E5%88%9D%E9%AB%94%E9%A9%97/</url>
    <content><![CDATA[<h2 id="PS-請先安裝好-node-js-應用程式-連結"><a href="#PS-請先安裝好-node-js-應用程式-連結" class="headerlink" title="PS. 請先安裝好 node.js 應用程式  連結"></a>PS. 請先安裝好 node.js 應用程式  <a href="https://nodejs.org/en/">連結</a></h2><h4 id="環境建立"><a href="#環境建立" class="headerlink" title="環境建立"></a>環境建立</h4><p>正式進入 Node 的世界，首先先在桌面建立一個資料夾 mock<br>接著在終端機輸入 npm init，接著一路 Enter 到底，這些都是可以做更改的，所以不用擔心!<br>init 完之後，mock 資料夾內會多一個 package.json 就代表成功了</p>
<hr>
<h4 id="第一步"><a href="#第一步" class="headerlink" title="第一步"></a>第一步</h4><p>接著試著引用 node 內建的 http 吧!<br>在 mock 資料夾內新增一個 index.js<br>接著來寫點 code</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> http = <span class="built_in">require</span>(<span class="string">&#x27;http&#x27;</span>);</span><br><span class="line"></span><br><span class="line">http.createServer(<span class="keyword">async</span> (req, res) =&gt; &#123;</span><br><span class="line">  res.end(<span class="string">&#x27;&lt;p&gt;Hello World!&lt;/p&gt;&#x27;</span>);</span><br><span class="line">&#125;).listen(<span class="number">3000</span>)</span><br></pre></td></tr></table></figure>
<p>這時候在終端機輸入 <code>node index.js</code> 啟用之後，可以在 local 的 3000 port 看到 Hello World! 就代表成功了</p>
<p>然而當回應的內容有修改時，都需重新輸入 node index.js，為了避免這個麻煩，可以使用 <a href="https://www.npmjs.com/package/nodemon">nodemon</a> 來協助專案</p>
<p>npm install -g nodemon 安裝好之後，在終端機輸入 <code>nodemon index.js</code>，這樣當回應的內容有修改時，就不需要每次都輸入 node index.js，只需在頁面做重整就好囉~</p>
<h4 id="模組化的引用"><a href="#模組化的引用" class="headerlink" title="模組化的引用"></a>模組化的引用</h4><p>當 JS 檔案變多的時候，會區分資料夾，這時候 node 也有模組化的匯出跟引用模式<br>假設我們想要讓頁面三秒後呈現，我們可以在 mock 資料夾內新增一個 utils 資料夾，裡面新增一個 sleep.js，接著寫點 setTimeout</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> sleep = <span class="function"><span class="params">ms</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">setTimeout</span>(resolve, ms);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">module</span>.exports = sleep</span><br></pre></td></tr></table></figure>
<p>最後的 <code>module.exports</code> 就是匯出的意思<br>接著回到 index.js require引入</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> sleep = <span class="built_in">require</span>(<span class="string">&quot;./utils/sleep&quot;</span>);</span><br></pre></td></tr></table></figure>
<p>這樣就完成了</p>
<hr>
]]></content>
      <categories>
        <category>node</category>
      </categories>
      <tags>
        <tag>node</tag>
      </tags>
  </entry>
  <entry>
    <title>Firebase 初體驗</title>
    <url>/2021/04/21/firebase%20%E5%88%9D%E9%AB%94%E9%A9%97/</url>
    <content><![CDATA[<p>firebase 為一款 Google 旗下的資料庫系統，不只跨足 Web，APP 也可以一起整合使用<br>會接觸到 firebase 是因為正好在學習 node 了解後端的部分，這就需要資料庫了，所以就來認識 firebase 了</p>
<h4 id="進入-Firebase-網站創建專案"><a href="#進入-Firebase-網站創建專案" class="headerlink" title="進入 Firebase 網站創建專案"></a>進入 Firebase 網站創建專案</h4><p>這裡因介面常常改版，所以不會截圖，但主要是進到 firebase 網站，利用 google 帳號登入，前往控制台，並新建一個專案的資料庫系統，firebase 有很多功能，但目前我用到的僅只有 database</p>
<h4 id="與自己的-html-檔案相連"><a href="#與自己的-html-檔案相連" class="headerlink" title="與自己的 html 檔案相連"></a>與自己的 html 檔案相連</h4><p>在自己本機的 html 檔案如果要跟 firebase 資料庫做相連的話，會取得一組 script，會長的如下:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- The core Firebase JS SDK is always required and must be listed first --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;https://www.gstatic.com/firebasejs/8.4.1/firebase-app.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;https://www.gstatic.com/firebasejs/8.4.1/firebase-database.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- <span class="doctag">TODO:</span> Add SDKs for Firebase products that you want to use</span></span><br><span class="line"><span class="comment">     https://firebase.google.com/docs/web/setup#available-libraries --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;https://www.gstatic.com/firebasejs/8.4.1/firebase-analytics.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">    <span class="comment">// Your web app&#x27;s Firebase configuration</span></span></span><br><span class="line"><span class="javascript">    <span class="comment">// For Firebase JS SDK v7.20.0 and later, measurementId is optional</span></span></span><br><span class="line"><span class="javascript">    <span class="keyword">var</span> firebaseConfig = &#123;</span></span><br><span class="line"><span class="javascript">      apiKey: <span class="string">&quot;xxxxxxxxx&quot;</span>,</span></span><br><span class="line"><span class="javascript">      authDomain: <span class="string">&quot;xxxxxxxxx&quot;</span>,</span></span><br><span class="line"><span class="javascript">      projectId: <span class="string">&quot;xxxxxxxxx&quot;</span>,</span></span><br><span class="line"><span class="javascript">      storageBucket: <span class="string">&quot;xxxxxxxxx&quot;</span>,</span></span><br><span class="line"><span class="javascript">      messagingSenderId: <span class="string">&quot;xxxxxxxxx&quot;</span>,</span></span><br><span class="line"><span class="javascript">      appId: <span class="string">&quot;xxxxxxxxx&quot;</span>,</span></span><br><span class="line"><span class="javascript">      measurementId: <span class="string">&quot;xxxxxxxxx&quot;</span></span></span><br><span class="line">    &#125;;</span><br><span class="line"><span class="javascript">    <span class="comment">// Initialize Firebase</span></span></span><br><span class="line">    firebase.initializeApp(firebaseConfig);</span><br><span class="line">    firebase.analytics();</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>這裡 xxxxxxxxx 的部分，類似於資料庫的帳密<br>這裡踩到的雷，有兩部分<br>firebase 因改版的關係，sdk 的功能沒有全部寫在一起了，所以僅只載入 firebase-app.js 的情況下用 database 是不行的，所以 database 的 js 要另外載入<br>第二點就是版本要全部統一，不然也會不能運行</p>
<h4 id="幫資料庫新增資料"><a href="#幫資料庫新增資料" class="headerlink" title="幫資料庫新增資料"></a>幫資料庫新增資料</h4><p>接著可以在本機的 html 的 script 寫點東西，幫資料庫新增資料</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">firebase.database().ref().set(<span class="string">&#x27;hello&#x27;</span>)</span><br></pre></td></tr></table></figure>
<p>這個時候在資料庫內就可以看到一組字串 hello<br>ref() 是指在根目錄<br>set() 是新增一筆資料</p>
<p>比較重要的一點就是，最外層不能接收陣列，可以是字串或物件，但不能是陣列<br>舉個例子:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> data = [];</span><br></pre></td></tr></table></figure>
<p>如果上述的方式添加到資料庫內，是會亂掉的，但資料的結構往往很複雜，這時候可以用物件格式在最外層，內層就可以採用陣列，如下:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> data = &#123;</span><br><span class="line">    food: [</span><br><span class="line">        &#123;</span><br><span class="line">            title: <span class="string">&#x27;&#x27;</span>,</span><br><span class="line">            content: <span class="string">&#x27;&#x27;</span></span><br><span class="line">        &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>如果要修改某一筆資料，ref() 的路徑跟資料結構就要對的起來，以上面的例子來說，我想更改 food 的 title，這個時候就會寫成這樣:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">firebase.database().ref(<span class="string">&#x27;food/0/title&#x27;</span>).set(內容)</span><br></pre></td></tr></table></figure>
<p>這樣資料庫的內容就可以被做更改了~~~</p>
]]></content>
      <categories>
        <category>firebase</category>
      </categories>
      <tags>
        <tag>firebase</tag>
      </tags>
  </entry>
  <entry>
    <title>Firebase 顯示資料到網頁上</title>
    <url>/2021/04/24/firebase%20%E9%A1%AF%E7%A4%BA%E8%B3%87%E6%96%99%E5%88%B0%E7%B6%B2%E9%A0%81%E4%B8%8A/</url>
    <content><![CDATA[<p>前一篇講到了在 firebase 內新建資料，那這些資料要如何呈現在網頁上呢？</p>
<h4 id="once-的應用"><a href="#once-的應用" class="headerlink" title="once 的應用"></a>once 的應用</h4><p>firebase 內有資料後，可以採用 once 這個方法拿取資料</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 先把路徑指到根目錄</span></span><br><span class="line"><span class="keyword">let</span> ref = firebase.database().ref();</span><br><span class="line"><span class="comment">// 提取 value </span></span><br><span class="line">ref.once(<span class="string">&#x27;value&#x27;</span>, <span class="function"><span class="keyword">function</span>(<span class="params">snapshot</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(snapshot.val());</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<h4 id="on-的應用與-once-的差異性"><a href="#on-的應用與-once-的差異性" class="headerlink" title="on 的應用與 once 的差異性"></a>on 的應用與 once 的差異性</h4><p>相較於 once，on 是即時的監聽資料庫內的資料，所以資料庫的內容一更改，頁面上的內容就會更改，無須重新整理，但語法跟 once 是一樣的</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 先把路徑指到根目錄</span></span><br><span class="line"><span class="keyword">let</span> ref = firebase.database().ref();</span><br><span class="line"><span class="comment">// 提取 value </span></span><br><span class="line">ref.on(<span class="string">&#x27;value&#x27;</span>, <span class="function"><span class="keyword">function</span>(<span class="params">snapshot</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(snapshot.val());</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>firebase</category>
      </categories>
      <tags>
        <tag>firebase</tag>
      </tags>
  </entry>
  <entry>
    <title>Firebase TodoList 實作</title>
    <url>/2021/04/24/firebase%20TodoList%20%E5%AF%A6%E4%BD%9C/</url>
    <content><![CDATA[<p>現在簡單的做一個 todo list 的功能，本機端寫入代辦事項，並記錄到 firebase，再從 firebase 撈取資料回來，渲染到畫面上</p>
<h4 id="HTML-架構"><a href="#HTML-架構" class="headerlink" title="HTML 架構"></a>HTML 架構</h4><p>首先先有一個大概的 HTML 架構，會如下:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;text&quot;</span> <span class="attr">placeholder</span>=<span class="string">&quot;請輸入代辦事項&quot;</span> <span class="attr">id</span>=<span class="string">&quot;text&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">button</span> <span class="attr">id</span>=<span class="string">&quot;send&quot;</span>&gt;</span>送出<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">&quot;list&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h4 id="選取-DOM-元素"><a href="#選取-DOM-元素" class="headerlink" title="選取 DOM 元素"></a>選取 DOM 元素</h4><p>接著依序選取 DOM 元素，並把 firebase 要存入資料的地方也存下來</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// todos 資料</span></span><br><span class="line"><span class="keyword">const</span> todos = firebase.database().ref(<span class="string">&#x27;todos&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 選取 DOM</span></span><br><span class="line"><span class="keyword">const</span> text = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;#text&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> send = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;#send&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> list = <span class="built_in">document</span>.querySelector(<span class="string">&#x27;#list&#x27;</span>);</span><br></pre></td></tr></table></figure>
<h4 id="按鈕送出事件"><a href="#按鈕送出事件" class="headerlink" title="按鈕送出事件"></a>按鈕送出事件</h4><p>接著按鈕綁上事件，送出之後寫入資料到 firebase</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 點選送出之後</span></span><br><span class="line">send.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 新增資料到 firebase</span></span><br><span class="line">   todos.push(&#123;</span><br><span class="line">    context: text.value</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<h4 id="撈取資料渲染到畫面上"><a href="#撈取資料渲染到畫面上" class="headerlink" title="撈取資料渲染到畫面上"></a>撈取資料渲染到畫面上</h4><p>上述的步驟都沒問題的話，可以檢查 firebase 是否有正常寫入資料，只要有正確寫入的話<br>接著只要從 firebase 撈取資料，渲染到畫面上就可以了</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 顯示資料出來</span></span><br><span class="line">todos.on(<span class="string">&#x27;value&#x27;</span>, <span class="function"><span class="keyword">function</span>(<span class="params">snapshot</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 從 firebase 撈回來的資料</span></span><br><span class="line">  <span class="keyword">const</span> data = snapshot.val();</span><br><span class="line">  <span class="comment">// 累加字串用</span></span><br><span class="line">  <span class="keyword">let</span> str = <span class="string">&#x27;&#x27;</span>;</span><br><span class="line">  <span class="comment">// 使用 for in 累加字串</span></span><br><span class="line">  <span class="keyword">for</span>(<span class="keyword">let</span> item <span class="keyword">in</span> data) &#123;</span><br><span class="line">    <span class="comment">// 綁上每筆資料的亂碼，做為每筆資料的 key，刪除可以拿來用</span></span><br><span class="line">    str += <span class="string">`&lt;li data-key=&quot;<span class="subst">$&#123;item&#125;</span>&quot;&gt;<span class="subst">$&#123;data[item].context&#125;</span>&lt;/li&gt;`</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 渲染到畫面上</span></span><br><span class="line">  list.innerHTML = str;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h4 id="刪除功能"><a href="#刪除功能" class="headerlink" title="刪除功能"></a>刪除功能</h4><p>接著點選畫面上的那筆資料，會刪除當筆資料</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 監聽整個 ul</span></span><br><span class="line">list.addEventListener(<span class="string">&#x27;click&#x27;</span>, <span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 如果 DOM 元素是 li 才執行</span></span><br><span class="line">  <span class="keyword">if</span>(e.target.nodeName === <span class="string">&#x27;LI&#x27;</span>) &#123;</span><br><span class="line">    <span class="comment">// 取出那筆 li 資料的 data-key</span></span><br><span class="line">    <span class="keyword">const</span> key = e.target.dataset.key;</span><br><span class="line">    <span class="comment">// 移除那筆資料</span></span><br><span class="line">    todos.child(key).remove();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>渲染畫面時，是採用 <code>.on()</code> 並非 <code>.once()</code> 所以刪除某筆資料時，畫面會及時更新<br>以上就是 todoList 的實作~</p>
]]></content>
      <categories>
        <category>firebase</category>
      </categories>
      <tags>
        <tag>firebase</tag>
      </tags>
  </entry>
  <entry>
    <title>Firebase 新增和移除資料</title>
    <url>/2021/04/24/firebase%20%E6%96%B0%E5%A2%9E%E5%92%8C%E7%A7%BB%E9%99%A4%E8%B3%87%E6%96%99/</url>
    <content><![CDATA[<p>前面有講到 <code>.set()</code> 新增資料到 firebase 內<br>假設 firebase 已經有資料了，要新增一筆資料到既有資料的 firebase 內，該怎麼做？<br>這時候可以使用 <code>.push()</code></p>
<h4 id="先找到根目錄"><a href="#先找到根目錄" class="headerlink" title="先找到根目錄"></a>先找到根目錄</h4><p>類似移動到根目錄</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> ref = firebase.database().ref();</span><br></pre></td></tr></table></figure>
<h4 id="善用-child-找到要新增資料的節點"><a href="#善用-child-找到要新增資料的節點" class="headerlink" title="善用 .child() 找到要新增資料的節點"></a>善用 .child() 找到要新增資料的節點</h4><p>可以運用 .child() 來找到要放置資料的地方，假設新增的資料要放在 about 底下，就可以這樣寫</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">ref.child(<span class="string">&#x27;about&#x27;</span>);</span><br></pre></td></tr></table></figure>
<h4 id="運用-push-新增資料"><a href="#運用-push-新增資料" class="headerlink" title="運用 .push() 新增資料"></a>運用 .push() 新增資料</h4><p>上面移動到 about 底下之後，就可以使用 .push() 來新增資料，為一個物件格式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">ref.child(<span class="string">&#x27;about&#x27;</span>).push(&#123; <span class="attr">test</span>: <span class="string">&#x27;test&#x27;</span> &#125;);</span><br></pre></td></tr></table></figure>
<p>這個時候到 firebase 看，about 底下就會多一筆物件資料，而且是有一組亂碼編號的，這個亂碼編號很重要，因為刪除就必須要使用到它</p>
<h4 id="remove-移除資料"><a href="#remove-移除資料" class="headerlink" title=".remove() 移除資料"></a>.remove() 移除資料</h4><p>上述提到的亂碼編號，就可以這樣寫來刪除資料:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// -MZ3F2vUp5n7KPLfa1ls 是我新增資料的亂碼編號</span></span><br><span class="line">ref.child(<span class="string">&#x27;about&#x27;</span>).child(<span class="string">&#x27;-MZ3F2vUp5n7KPLfa1ls&#x27;</span>).remove();</span><br></pre></td></tr></table></figure>
<p>上述的方法，融會貫通之後，可以交錯的使用來新增移除資料</p>
]]></content>
      <categories>
        <category>firebase</category>
      </categories>
      <tags>
        <tag>firebase</tag>
      </tags>
  </entry>
  <entry>
    <title>Firebase 資料排序</title>
    <url>/2021/04/26/firebase-%E8%B3%87%E6%96%99%E6%8E%92%E5%BA%8F/</url>
    <content><![CDATA[<p>這裡要來說明資料在 firebase 時，如何能依照指定的屬性數值大小，從小到大做排序</p>
<h4 id="寫入資料到-firebase"><a href="#寫入資料到-firebase" class="headerlink" title="寫入資料到 firebase"></a>寫入資料到 firebase</h4><p>首先，先把資料寫入到 firebase，格式會如下:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> people = &#123;</span><br><span class="line">  <span class="string">&#x27;bob&#x27;</span>: &#123;</span><br><span class="line">    weight: <span class="number">45</span>,</span><br><span class="line">    age: <span class="number">20</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">&#x27;John&#x27;</span>: &#123;</span><br><span class="line">    weight: <span class="number">50</span>,</span><br><span class="line">    age: <span class="number">15</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">&#x27;mike&#x27;</span>: &#123;</span><br><span class="line">    weight: <span class="number">60</span>,</span><br><span class="line">    age: <span class="number">30</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> peopleRef = firebase.database().ref(<span class="string">&#x27;people&#x27;</span>);</span><br><span class="line">peopleRef.set(people);</span><br></pre></td></tr></table></figure>
<hr>
<h4 id="運用-orderByChild-搭配-forEach-把資料做排序"><a href="#運用-orderByChild-搭配-forEach-把資料做排序" class="headerlink" title="運用 .orderByChild() 搭配 .forEach() 把資料做排序"></a>運用 .orderByChild() 搭配 .forEach() 把資料做排序</h4><p>假設現在我們希望資料的排序能依照 age 屬性的值從小到大做排序的話，可以運用 .orderByChild() 搭配 .forEach()</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">peopleRef.orderByChild(<span class="string">&#x27;age&#x27;</span>).once(<span class="string">&#x27;value&#x27;</span>, <span class="function"><span class="keyword">function</span>(<span class="params">snapshot</span>) </span>&#123;</span><br><span class="line">  snapshot.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">item</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(item.val());</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>這時候在 console 看，資料就會照我們期望的樣子做排序</p>
<hr>
<h4 id="orderByChild-排序規則"><a href="#orderByChild-排序規則" class="headerlink" title=".orderByChild() 排序規則"></a>.orderByChild() 排序規則</h4><p><code>.orderByChild()</code> 有一套排序規則，當今天每一筆資料的屬性內的值，若型別都不一樣時，排序規則會如下:</p>
<ol>
<li>null </li>
<li>false</li>
<li>true</li>
<li>number 從小到大</li>
<li>字串</li>
<li>其他: 例如物件</li>
</ol>
<p>這裡附上<a href="https://firebase.google.com/docs/database/web/lists-of-data#sort_data">連結</a></p>
]]></content>
      <categories>
        <category>firebase</category>
      </categories>
      <tags>
        <tag>firebase</tag>
      </tags>
  </entry>
  <entry>
    <title>Firebase 資料搜尋區間</title>
    <url>/2021/04/26/Firebase-%E8%B3%87%E6%96%99%E6%90%9C%E5%B0%8B%E5%8D%80%E9%96%93/</url>
    <content><![CDATA[<p>這裡來指定 firebase 內的資料按照我們要的區間來搜尋</p>
<h4 id="寫入資料"><a href="#寫入資料" class="headerlink" title="寫入資料"></a>寫入資料</h4><p>這裡更換一下資料，一樣寫入到 firebase 內</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> people = &#123;</span><br><span class="line">        <span class="string">&quot;mike&quot;</span>: &#123;</span><br><span class="line">          <span class="string">&quot;height&quot;</span> : <span class="number">4</span>,</span><br><span class="line">          <span class="string">&quot;length&quot;</span> : <span class="number">12.5</span>,</span><br><span class="line">          <span class="string">&quot;weight&quot;</span>: <span class="number">5000</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;casper&quot;</span>: &#123;</span><br><span class="line">          <span class="string">&quot;height&quot;</span> : <span class="number">3</span>,</span><br><span class="line">          <span class="string">&quot;length&quot;</span> : <span class="number">9</span>,</span><br><span class="line">          <span class="string">&quot;weight&quot;</span> : <span class="number">2500</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;bob&quot;</span>: &#123;</span><br><span class="line">          <span class="string">&quot;height&quot;</span>: <span class="string">&quot;dalse&quot;</span>,</span><br><span class="line">          <span class="string">&quot;length&quot;</span> : <span class="literal">false</span>,</span><br><span class="line">          <span class="string">&quot;weight&quot;</span> : <span class="number">3500</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;john&quot;</span>: &#123;</span><br><span class="line">          <span class="string">&quot;height&quot;</span> : <span class="literal">true</span>,</span><br><span class="line">          <span class="string">&quot;length&quot;</span> : <span class="number">9</span>,</span><br><span class="line">          <span class="string">&quot;weight&quot;</span> : <span class="number">2500</span></span><br><span class="line">        &#125;</span><br><span class="line">        ,</span><br><span class="line">        <span class="string">&quot;josh&quot;</span>: &#123;</span><br><span class="line">          <span class="string">&quot;height&quot;</span> : <span class="literal">false</span>,</span><br><span class="line">          <span class="string">&quot;length&quot;</span> : <span class="number">9</span>,</span><br><span class="line">          <span class="string">&quot;weight&quot;</span> : <span class="number">2500</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&#x27;boss&#x27;</span>:&#123;</span><br><span class="line">          <span class="string">&quot;length&quot;</span>: <span class="number">3</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&#x27;frank&#x27;</span>:&#123;</span><br><span class="line">          height:&#123;<span class="string">&#x27;aaa&#x27;</span>:<span class="number">1</span>&#125;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> peopleRef = firebase.database().ref(<span class="string">&#x27;people&#x27;</span>);</span><br><span class="line">peopleRef.set(people);</span><br></pre></td></tr></table></figure>
<hr>
<h4 id="資料搜尋區間"><a href="#資料搜尋區間" class="headerlink" title="資料搜尋區間"></a>資料搜尋區間</h4><p>假設資料我們需要的是某一個區間的值，這時候可以採用 <code>.stratAt()</code> <code>.endAt()</code><br>如果上述的資料，希望抓出 weight 值為 3500 ~ 5000，就可以這樣寫:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">peopleRef.orderByChild(<span class="string">&#x27;weight&#x27;</span>).startAt(<span class="number">3500</span>).endAt(<span class="number">5000</span>).once(<span class="string">&#x27;value&#x27;</span>, <span class="function"><span class="keyword">function</span>(<span class="params">snapshot</span>) </span>&#123;</span><br><span class="line">  snapshot.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">item</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 可以運用 key 抓出資料的父層</span></span><br><span class="line">    <span class="built_in">console</span>.log(item.key, item.val());</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>這時候就會抓出 bob 和 mike 兩筆資料</p>
<hr>
<h4 id="資料剛好等於某個值"><a href="#資料剛好等於某個值" class="headerlink" title="資料剛好等於某個值"></a>資料剛好等於某個值</h4><p>有時候成千上萬筆資料中，我們剛好知道某個值，可以藉此下去搜尋資料<br>例如: email<br>那上述的資料，希望抓出 weight 為 5000 的資料，就可以使用 <code>.equalTo()</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">peopleRef.orderByChild(<span class="string">&#x27;weight&#x27;</span>).equalTo(<span class="number">5000</span>).once(<span class="string">&#x27;value&#x27;</span>, <span class="function"><span class="keyword">function</span>(<span class="params">snapshot</span>) </span>&#123;</span><br><span class="line">  snapshot.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">item</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(item.key, item.val());</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>這時候就會抓出 mike 這筆資料</p>
<hr>
<h4 id="限制筆數"><a href="#限制筆數" class="headerlink" title="限制筆數"></a>限制筆數</h4><p>假設資料成千上萬筆，且都有符合要的條件，這時候可以給予限制要抓取的筆數<br>而且可以從最前面符合的資料開始抓 <code>.limitToFirst(要抓取的筆數)</code><br>或者從最後面符合的資料開始抓 <code>.limitToLast(要抓取的筆數)</code><br>假設我們要抓取 weight 值為 2500，且從最前面開始抓取一筆資料:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">peopleRef.orderByChild(<span class="string">&#x27;weight&#x27;</span>).equalTo(<span class="number">2500</span>).limitToFirst(<span class="number">1</span>).once(<span class="string">&#x27;value&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">snapshot</span>) </span>&#123;</span><br><span class="line">  snapshot.forEach(<span class="function"><span class="keyword">function</span> (<span class="params">item</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(item.key, item.val());</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>這時候 console 會是 casper 這筆資料<br>如果反過來從後面開始抓呢:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">peopleRef.orderByChild(<span class="string">&#x27;weight&#x27;</span>).equalTo(<span class="number">2500</span>).limitToLast(<span class="number">1</span>).once(<span class="string">&#x27;value&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">snapshot</span>) </span>&#123;</span><br><span class="line">  snapshot.forEach(<span class="function"><span class="keyword">function</span> (<span class="params">item</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(item.key, item.val());</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>這時候 console 就會是 josh 這筆資料了</p>
]]></content>
      <categories>
        <category>firebase</category>
      </categories>
      <tags>
        <tag>firebase</tag>
      </tags>
  </entry>
</search>
